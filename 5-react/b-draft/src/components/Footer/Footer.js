import React from 'react';
import { hot } from 'react-hot-loader';
import './footer.scss';

class Footer extends React.Component {
  render() {
    return (
      <div className="nass-footer-component"></div>
    );
  }
}

export default hot(module)(Footer);
