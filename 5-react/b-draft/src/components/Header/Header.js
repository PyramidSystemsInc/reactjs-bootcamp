import React from 'react';
import { hot } from 'react-hot-loader';
import './header.scss';

class Header extends React.Component {
  render() {
    return (
      <div className="nass-header-component"></div>
    );
  }
}

export default hot(module)(Header);
