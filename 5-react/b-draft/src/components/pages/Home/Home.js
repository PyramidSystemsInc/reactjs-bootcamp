import React from 'react';
import { hot } from 'react-hot-loader';
import './home.scss';

class Home extends React.Component {
  render() {
    return (
      <div className="nass-home-component"></div>
    );
  }
}

export default hot(module)(Home);
